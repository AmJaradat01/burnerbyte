# Implementation Plan

- [ ] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Invite Team Assignment Ignored
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the invite team assignment bugs exist
  - **Scoped PBT Approach**: For each defective code path, scope the property to concrete failing cases that reproduce the specific bug
  - Create a test file `internal/service/org_service_invite_test.go`
  - Extract pure logic functions that mirror the service behavior for testability (same pattern as existing tests in `internal/auth/middleware_test.go` and `internal/worker/cleanup_test.go`)
  - Test group 1 — AcceptInvite ignores team_id (requirement 1.1):
    - Extract a `processAcceptInvite` function that takes an invite and returns the set of memberships created
    - Model the CURRENT (buggy) behavior: function creates org membership but never creates team membership even when `invite.TeamID != nil`
    - Write property-based test using `pgregory.net/rapid`: for any invite where `TeamID != nil` and `TeamRole != nil`, assert that `processAcceptInvite` creates BOTH org membership AND team membership
    - Generate random invites with non-nil `TeamID` and valid `TeamRole` values (`"member"`, `"lead"`, `"viewer"`)
    - The test will FAIL on unfixed code because the current logic only creates org membership
  - Test group 2 — InviteMember accepts cross-org team_id (requirement 1.2):
    - Extract a `validateInviteTeam` function that checks whether a team belongs to the target org
    - Model the CURRENT (buggy) behavior: function does NOT validate team ownership
    - Write property-based test: for any invite where `team.OrgID != input.OrgID`, assert that `validateInviteTeam` returns an error
    - Generate random org IDs and team org IDs that differ, verify validation rejects them
    - The test will FAIL on unfixed code because no validation exists
  - Test group 3 — InviteMember does not default team_role (requirement 1.3):
    - Extract a `resolveTeamRole` function that defaults nil `TeamRole` to `"member"` when `TeamID` is provided
    - Model the CURRENT (buggy) behavior: function returns nil when `TeamRole` is nil
    - Write property-based test: for any invite where `TeamID != nil` and `TeamRole == nil`, assert that `resolveTeamRole` returns `"member"`
    - The test will FAIL on unfixed code because no defaulting logic exists
  - Test group 4 — PreviewInvite omits team info (requirement 1.4):
    - Extract a `buildPreviewResponse` function that constructs the preview map from invite + org + team data
    - Model the CURRENT (buggy) behavior: function only includes `email`, `org_name`, `org_role`
    - Write property-based test: for any invite where `TeamID != nil`, assert that the preview map contains `team_name` and `team_role` keys
    - The test will FAIL on unfixed code because team fields are never included
  - Test group 5 — AcceptInvite handler omits team audit metadata (requirement 1.8):
    - Extract a `buildAcceptAuditMetadata` function that constructs the audit metadata map
    - Model the CURRENT (buggy) behavior: function only includes `email`, `org_name`, `org_id`
    - Write property-based test: for any invite where `TeamID != nil`, assert that audit metadata contains `team_id`, `team_name`, `team_role`
    - The test will FAIL on unfixed code because team fields are never included in audit
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests FAIL (this is correct - it proves the bugs exist)
  - Document counterexamples found to understand root cause
  - Mark task complete when tests are written, run, and failures are documented
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.8_

- [ ] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Org-Only Invite Behavior Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Create a test file `internal/service/org_service_invite_preservation_test.go`
  - Observe behavior on UNFIXED code for all org-only invite flows (where `TeamID == nil`), then write property-based tests asserting observed behavior is preserved:
  - Observe: `processAcceptInvite` with `invite.TeamID == nil` creates only org membership — write property-based test using `rapid` generating random org-only invites with varied `OrgRole` values (`"owner"`, `"admin"`, `"member"`), asserting only org membership is created and no team membership is created
  - Observe: `processAcceptInvite` with expired invite returns `"invite expired"` error — write property-based test generating random expired timestamps (1 minute to 365 days in the past), asserting error is returned
  - Observe: `processAcceptInvite` with email mismatch returns `"email mismatch"` error — write property-based test generating random email pairs that differ, asserting error is returned
  - Observe: `processAcceptInvite` with already-accepted invite returns org info without error (idempotent) — write property-based test generating random already-accepted invites, asserting no error and correct org info returned
  - Observe: `buildPreviewResponse` with `invite.TeamID == nil` returns map with exactly `email`, `org_name`, `org_role` keys and no team fields — write property-based test generating random org-only invites with varied org names and roles, asserting exact key set
  - Observe: `resolveTeamRole` with `TeamID == nil` returns nil regardless of `TeamRole` value — write property-based test generating random nil/non-nil `TeamRole` values with nil `TeamID`, asserting nil is returned
  - Observe: `validateInviteTeam` with `TeamID == nil` returns no error (skips validation) — write property-based test generating random org IDs with nil `TeamID`, asserting no error
  - Observe: `buildAcceptAuditMetadata` with `invite.TeamID == nil` returns map with `email`, `org_name`, `org_id` and no team fields — write property-based test asserting exact key set
  - Observe: `buildInviteAuditMetadata` with `input.TeamID == nil` returns map with `email`, `role`, `org_name` and no team fields — write property-based test asserting exact key set
  - Verify all preservation tests PASS on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7, 3.8_

- [ ] 3. Fix invite team assignment

  - [ ] 3.1 Add `teamRepo` dependency to `OrgService`
    - In `internal/service/org_service.go`, add `teamRepo *postgres.TeamRepo` field to the `OrgService` struct
    - Update `NewOrgService` constructor signature to accept `teamRepo *postgres.TeamRepo` parameter and store it
    - _Bug_Condition: isBugCondition(invite) where invite.TeamID IS NOT NULL — OrgService cannot access team data without teamRepo_
    - _Expected_Behavior: OrgService has access to teamRepo for team validation and membership creation_
    - _Preservation: All existing OrgService methods continue to work unchanged since teamRepo is only used in new code paths_
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6, 2.7, 2.9_

  - [ ] 3.2 Update `NewOrgService` call site in `cmd/api/main.go`
    - In `cmd/api/main.go`, update the `service.NewOrgService(...)` call to pass `teamRepo` as the new parameter
    - The `teamRepo` variable is already instantiated in main.go (`teamRepo := postgres.NewTeamRepo(pool)`)
    - Change: `service.NewOrgService(pool, orgRepo, ml, cfg.Server.FrontendURL, cfg.Defaults.InviteExpiryTTL)`
    - To: `service.NewOrgService(pool, orgRepo, teamRepo, ml, cfg.Server.FrontendURL, cfg.Defaults.InviteExpiryTTL)`
    - _Bug_Condition: isBugCondition(invite) where invite.TeamID IS NOT NULL — call site must pass teamRepo_
    - _Expected_Behavior: OrgService is constructed with teamRepo dependency_
    - _Preservation: No behavioral change — only wiring a new dependency_
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6, 2.7, 2.9_

  - [ ] 3.3 Fix `InviteMember` — validate team belongs to org and default `team_role`
    - In `internal/service/org_service.go`, `InviteMember` method
    - After parsing `team_id` into `teamID`, look up the team via `s.teamRepo.GetByID(ctx, id)`
    - Verify `team.OrgID == orgID`; if not, return error `"team does not belong to this organization"`
    - If `input.TeamRole` is nil but `input.TeamID` is provided, default `team_role` to `"member"` by setting `invite.TeamRole` to a pointer to `"member"`
    - Optionally validate `team_role` with `rbac.ValidTeamRole(*input.TeamRole)` and return error if invalid
    - _Bug_Condition: isBugCondition(invite) where invite.TeamID IS NOT NULL — currently no validation of team ownership or team_role defaulting_
    - _Expected_Behavior: InviteMember validates team.OrgID == orgID and defaults team_role to "member" when nil_
    - _Preservation: Invites without team_id skip all team validation (unchanged)_
    - _Requirements: 2.2, 2.3_

  - [ ] 3.4 Fix `AcceptInvite` — create team membership in same transaction
    - In `internal/service/org_service.go`, `AcceptInvite` method
    - After creating org membership (or handling the already-member case) and before committing the transaction, check if `invite.TeamID != nil`
    - If team_id is present:
      - Look up the team via `s.teamRepo.WithTx(tx).GetByID(ctx, *invite.TeamID)` to verify it still exists
      - If team is not found (deleted after invite creation), log a warning with `slog.Warn` and skip team assignment — do NOT fail the transaction
      - If team is archived (`team.IsArchived == true`), log a warning with `slog.Warn` and skip team assignment
      - Create team membership via `s.teamRepo.WithTx(tx).CreateMembership(ctx, &domain.TeamMembership{ID: uuid.New(), UserID: userID, TeamID: *invite.TeamID, Role: *invite.TeamRole})`
      - If `CreateMembership` returns `postgres.ErrConflict` (user already a team member), skip silently — idempotent behavior
      - All within the existing transaction so org membership + team membership are atomic
    - Update the return signature to include team info: change from `(uuid.UUID, string, error)` to `(uuid.UUID, string, *uuid.UUID, string, string, error)` returning `(orgID, orgName, teamID, teamName, teamRole, error)` — OR return a result struct
    - Alternatively, return a struct `AcceptInviteResult` with `OrgID`, `OrgName`, `TeamID`, `TeamName`, `TeamRole` fields for cleaner API
    - _Bug_Condition: isBugCondition(invite) where invite.TeamID IS NOT NULL — AcceptInvite reads TeamID/TeamRole but never uses them_
    - _Expected_Behavior: AcceptInvite creates team membership atomically with org membership, handles deleted/archived teams gracefully_
    - _Preservation: Invites without team_id follow the exact same code path as before (unchanged)_
    - _Requirements: 2.1, 2.5, 2.6, 2.7_

  - [ ] 3.5 Fix `PreviewInvite` — include team name and team_role
    - In `internal/service/org_service.go`, `PreviewInvite` method
    - After building the base response map, check if `invite.TeamID != nil`
    - If team_id is present, look up the team via `s.teamRepo.GetByID(ctx, *invite.TeamID)`
    - If team is found, add `"team_name": team.Name` and `"team_role": *invite.TeamRole` to the response map
    - If team is not found (deleted), omit team fields or add `"team_name": "(deleted)"` — graceful degradation
    - _Bug_Condition: isBugCondition(invite) where invite.TeamID IS NOT NULL — PreviewInvite only returns org fields_
    - _Expected_Behavior: PreviewInvite includes team_name and team_role when invite has team_id_
    - _Preservation: Org-only invites return exactly the same response as before (unchanged)_
    - _Requirements: 2.4_

  - [ ] 3.6 Fix `ListPendingInvites` — enrich with team names
    - In `internal/service/org_service.go`, `ListPendingInvites` method (or in the handler)
    - After fetching pending invites, collect unique non-nil `team_id` values
    - Batch-look up teams by ID using `s.teamRepo.GetByID` for each unique team_id
    - Add a `TeamName` field to the `Invite` domain struct in `internal/domain/org.go` (as a joined/enriched field, similar to `Email` and `DisplayName` on `OrgMembership`)
    - Populate `invite.TeamName` for each invite that has a `team_id`
    - Alternatively, return an enriched response type from the service or handler
    - _Bug_Condition: isBugCondition(invite) where invite.TeamID IS NOT NULL — ListPendingInvites returns raw invites without team names_
    - _Expected_Behavior: ListPendingInvites includes team_name for each invite with team_id_
    - _Preservation: Org-only invites are unaffected — TeamName remains empty/omitted_
    - _Requirements: 2.9_

  - [ ] 3.7 Fix `AcceptInvite` handler — include team metadata in audit
    - In `internal/handler/org.go`, `AcceptInvite` method
    - Update the handler to use the new return values from `OrgService.AcceptInvite` (which now returns team info)
    - If team info is present (teamID is non-nil), add `"team_id"`, `"team_name"`, and `"team_role"` to the audit metadata map
    - Current: `map[string]any{"email": uc.Email, "org_name": orgName, "org_id": orgID.String()}`
    - Expected (when team present): `map[string]any{"email": uc.Email, "org_name": orgName, "org_id": orgID.String(), "team_id": teamID.String(), "team_name": teamName, "team_role": teamRole}`
    - _Bug_Condition: isBugCondition(invite) where invite.TeamID IS NOT NULL — handler omits team metadata from audit_
    - _Expected_Behavior: Audit metadata includes team_id, team_name, team_role when invite has team assignment_
    - _Preservation: Org-only invite acceptance audit metadata unchanged_
    - _Requirements: 2.8_

  - [ ] 3.8 Fix `InviteMember` handler — include team info in audit
    - In `internal/handler/org.go`, `InviteMember` method
    - After creating the invite, if `input.TeamID` is non-nil, add `"team_id"` and `"team_role"` to the audit metadata map
    - Current: `map[string]any{"email": input.Email, "role": input.OrgRole, "org_name": orgName}`
    - Expected (when team present): `map[string]any{"email": input.Email, "role": input.OrgRole, "org_name": orgName, "team_id": *input.TeamID, "team_role": teamRole}`
    - _Bug_Condition: isBugCondition(invite) where invite.TeamID IS NOT NULL — handler omits team info from invite creation audit_
    - _Expected_Behavior: Audit metadata includes team_id and team_role when invite has team assignment_
    - _Preservation: Org-only invite creation audit metadata unchanged_
    - _Requirements: 2.8_

  - [ ] 3.9 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Invite Team Assignment Works
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.8_

  - [ ] 3.10 Verify preservation tests still pass
    - **Property 2: Preservation** - Org-Only Invite Behavior Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions)

- [ ] 4. Checkpoint - Ensure all tests pass
  - Run `go build ./...` to verify the project compiles
  - Run `go test ./internal/service/... ./internal/handler/... ./internal/worker/... ./internal/auth/...` to verify all tests pass
  - Ensure all property-based tests (bug condition + preservation) pass
  - Ensure no regressions in existing tests
  - Ask the user if questions arise
